import React from 'react';

const users = () => {
    return (
        <div>
            
        </div>
    );
};

export default users;
users.getLayout = function PageLayout(page: any) {
  return <>{page}</>;
};
