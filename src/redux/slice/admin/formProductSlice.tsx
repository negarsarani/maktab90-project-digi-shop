import { createSlice, PayloadAction } from '@reduxjs/toolkit';
const initialState = {};
export const formProductSlice = createSlice({
  name: 'admin',
  initialState,
  reducers: {},
});
export const {} = formProductSlice.actions;
export default formProductSlice.reducer;
